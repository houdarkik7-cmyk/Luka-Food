[build]
  command = "pnpm -r --if-present run build"
  publish = "artifacts/luka-food/dist/public/"

[build.environment]
  PORT = "8080"
  BASE_PATH = "/"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
